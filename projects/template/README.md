# Project Template

**한국어 · English**

- **KO** — 모든 랩이 링크하는 공유 프레임워크(`lab::` 네임스페이스). `App` 베이스 클래스, Vulkan context, swapchain, command buffer, 동기화 프리미티브 제공.
- **EN** — Shared framework every lab links (`lab::` namespace). Provides the `App` base class, Vulkan context, swapchain, command buffers, and synchronization primitives.
